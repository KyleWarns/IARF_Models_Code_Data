within ;
package IARF_Models "Collection of models used for generating data for testing of IARF"
annotation (uses(
    TRANSFORM(version="0.5"),
    Modelica(version="4.0.0"),
    UserInteraction(version="0.70")));
end IARF_Models;
